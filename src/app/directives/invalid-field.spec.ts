import { InvalidField } from './invalid-field';

describe('InvalidField', () => {
  it('should create an instance', () => {
    const directive = new InvalidField();
    expect(directive).toBeTruthy();
  });
});
